{
  "extends": ["stylelint-config-standard-scss", "stylelint-config-recess-order"],
  "rules": {
    "selector-max-id": 0,
    "selector-class-pattern": "^[a-zA-Z0-9]+(__[a-z0-9A-Z_]+)?$",
    "max-nesting-depth": [
      1,
      {
        "ignore": ["pseudo-classes"]
      }
    ]
  }
}
